delete:
  faker:
    code:
      isbn_gs1: ["3","2"]
      isbn_group: ["1"]
      isbn_registrant: ["#{numerify '#-####'}"]
    house:
       dummy: [Andrew, J, R]
       simple: "bye"
       depricateResolution: "#{bye}"
       denyResolution: "#{Superhero.name}"
       regeify1: "#{regeify '[45]{2}'}"
       regeify_cell: "#{regeify '4[57]9'}"
       regeify_slash_format: /[45]{2}/
       decrepify_2: "#{botify '??##','false'}"
       sameResolution: "#{bye} #{bye}"
       multipleResolution: "#{bye} #{Superhero.descriptor}"
       resolutionWithList:
           - "#{bye}"
           - "#{bye2}"
