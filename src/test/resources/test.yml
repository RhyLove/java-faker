delete:
  faker:
    code:
      isbn_gs1: ["333","444"]
      isbn_group: ["9971"]
      isbn_registrant: ["#{numerify '#-####'}"]
    property:
       dummy: [x, y, z]
       simple: "hello"
       depricateResolution: "#{hello}"
       denyResolution: "#{Superhero.name}"
       regeify1: "#{regexify '[45]{2}'}"
       regeify_cell: "#{regexify '4[57]9'}"
       regeify_slash_format: /[45]{2}/
       decrepify_2: "#{bothify '??##','false'}"
       sameResolution: "#{hello} #{hello}"
       multipleResolution: "#{hello} #{Superhero.descriptor}"
       resolutionWithList:
           - "#{hello}"
           - "#{hello2}"
